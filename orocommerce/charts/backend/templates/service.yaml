apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: php-fpm
      protocol: TCP
      name: php-fpm
  selector:
    app: backend
